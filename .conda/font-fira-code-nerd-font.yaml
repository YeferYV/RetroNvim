# https://rattler-build.prefix.dev/latest/tutorials/repackaging
# rattler-build build   --recipe font-fira-code-nerd-font.yaml --target-platform linux-64      --output-dir firacode-channel
# rattler-build build   --recipe font-fira-code-nerd-font.yaml --target-platform linux-aarch64 --output-dir firacode-channel
# rattler-build build   --recipe font-fira-code-nerd-font.yaml --target-platform osx-64        --output-dir firacode-channel
# rattler-build build   --recipe font-fira-code-nerd-font.yaml --target-platform osx-arm64     --output-dir firacode-channel
# rattler-build build   --recipe font-fira-code-nerd-font.yaml --target-platform win-64        --output-dir firacode-channel

# https://pixi.prefix.dev/dev/reference/cli/pixi/clean/cache
# pixi clean cache --build --yes

# https://pixi.prefix.dev/dev/reference/cli/pixi/global/install
# pixi g install --path ./firacode-channel/linux-64/*conda
# pixi g install --path ./firacode-channel/linux-aarch64/*conda
# pixi g install --path ./firacode-channel/osx-64/*conda
# pixi g install --path ./firacode-channel/osx-aarch64/*conda
# pixi g install --path ./firacode-channel/win-64/*conda

# https://rattler-build.prefix.dev/latest/getting_started/#authentication
# rattler-build auth login anaconda.org --conda-token $ANACONDA_API_KEY

# https://rattler-build.prefix.dev/latest/getting_started/#publishing-the-package
# rattler-build publish --recipe font-fira-code-nerd-font.yaml --target-platform linux-64      --to https://anaconda.org/retronvim
# rattler-build publish --recipe font-fira-code-nerd-font.yaml --target-platform linux-aarch64 --to https://anaconda.org/retronvim
# rattler-build publish --recipe font-fira-code-nerd-font.yaml --target-platform osx-64        --to https://anaconda.org/retronvim
# rattler-build publish --recipe font-fira-code-nerd-font.yaml --target-platform osx-arm64     --to https://anaconda.org/retronvim
# rattler-build publish --recipe font-fira-code-nerd-font.yaml --target-platform win-64        --to https://anaconda.org/retronvim

context:
  version: 3.4.0

package:
  name: font-fira-code-nerd-font
  version: ${{ version }}

source:
  - url: https://github.com/ryanoasis/nerd-fonts/releases/download/v${{ version }}/firacode.tar.xz
    target_directory: bin/patched-fonts/FiraCode

  - url: https://raw.githubusercontent.com/ryanoasis/nerd-fonts/refs/heads/master/install.sh

  - url: https://raw.githubusercontent.com/ryanoasis/nerd-fonts/refs/heads/master/install.ps1


build:
  # number: 0 # build version

  # noarch: generic # doesn't expose $PREFIX/bin/firacode-nerdfont-installer.cmd on windows

  dynamic_linking:
    # fixes macos builds from linux: failed to find `install_name_tool`
    # https://rattler-build.prefix.dev/latest/build_options/#dynamic-linking-configuration
    binary_relocation: false

  script:
    interpreter: bash
    content:
      - cp -r bin      $PREFIX

      # UNIX
      - cp install.sh  $PREFIX/bin/firacode-nerdfont-installer
      - chmod +x       $PREFIX/bin/firacode-nerdfont-installer

      # WINDOWS
      - cp install.ps1 $PREFIX/bin/firacode-nerdfont-installer.ps1
      - cat >>         $PREFIX/bin/firacode-nerdfont-installer.cmd <<< '@echo off'
      - cat >>         $PREFIX/bin/firacode-nerdfont-installer.cmd <<< "set CONDA_PREFIX=%~f0\..\.."
      - cat >>         $PREFIX/bin/firacode-nerdfont-installer.cmd <<< "powershell -executionpolicy bypass -c %CONDA_PREFIX%/bin/firacode-nerdfont-installer.ps1"

about:
  summary: Iconic font aggregator, collection, and patcher. 3,600+ icons, 50+ patched fonts
  description: |
    Nerd Fonts is a project that patches developer targeted fonts with a high number of glyphs (icons).
    Specifically to add a high number of extra glyphs from popular 'iconic fonts'
    such as Font Awesome, Devicons, Octicons, and others.
  homepage: https://nerdfonts.com
  repository: https://github.com/ryanoasis/nerd-fonts
